{
  "name": "rewrite-app",
  "private": true,
  "workspaces": [
    "frontend"
  ],
  "scripts": {
    "build": "npm install && npm --prefix frontend install && npm --prefix frontend run build"
  }
}
